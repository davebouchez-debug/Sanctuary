"""Sanctuary Didactic Firewall — Presence Protection Layer.

Standalone, side-effect-free modules. Nothing here touches the live response
path until explicitly wired in. Build order:
  1. presence_break_detector  (this slice — the categorical tripwire)
"""
